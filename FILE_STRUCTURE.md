# 项目文件结构

## 📁 完整目录结构

```
jenkins-custom-platform/
│
├── README.md                          # 项目说明文档
├── DEVELOPMENT_GUIDE.md               # 开发指南
├── PROJECT_SUMMARY.md                 # 项目总结
├── FILE_STRUCTURE.md                  # 本文件
│
├── jenkins-platform-backend/          # 后端服务
│   ├── src/                          # 源代码目录
│   │   ├── app.ts                    # Express 应用入口
│   │   │
│   │   ├── config/                   # 配置文件
│   │   │   └── jenkins.config.ts     # Jenkins 连接配置
│   │   │
│   │   ├── types/                    # TypeScript 类型定义
│   │   │   └── index.ts              # 公共类型定义
│   │   │
│   │   ├── services/                 # 业务逻辑层
│   │   │   ├── jenkins.service.ts    # Jenkins API 封装
│   │   │   └── auth.service.ts       # 认证服务
│   │   │
│   │   ├── controllers/              # 控制器层
│   │   │   ├── job.controller.ts     # 任务控制器
│   │   │   └── auth.controller.ts    # 认证控制器
│   │   │
│   │   ├── routes/                   # 路由定义
│   │   │   ├── job.routes.ts         # 任务路由
│   │   │   └── auth.routes.ts        # 认证路由
│   │   │
│   │   └── middleware/               # 中间件
│   │       ├── auth.middleware.ts    # 认证中间件
│   │       └── error.middleware.ts   # 错误处理中间件
│   │
│   ├── package.json                  # 后端依赖配置
│   ├── tsconfig.json                 # TypeScript 配置
│   └── .env.example                  # 环境变量示例
│
└── jenkins-platform-frontend/         # 前端应用
    ├── src/                          # 源代码目录
    │   ├── main.tsx                  # 应用入口
    │   ├── App.tsx                   # 根组件
    │   ├── index.css                 # 全局样式
    │   │
    │   ├── api/                      # API 封装
    │   │   ├── client.ts             # HTTP 客户端
    │   │   ├── auth.ts               # 认证 API
    │   │   └── jobs.ts               # 任务 API
    │   │
    │   ├── store/                    # 状态管理
    │   │   └── index.ts              # Zustand Store
    │   │
    │   ├── types/                    # TypeScript 类型
    │   │   └── index.ts              # 公共类型定义
    │   │
    │   ├── components/               # React 组件
    │   │   ├── layout/               # 布局组件
    │   │   ├── common/               # 通用组件
    │   │   └── job/                  # 任务相关组件
    │   │
    │   └── pages/                    # 页面组件
    │       ├── Login/                # 登录页
    │       ├── Dashboard/            # 仪表盘
    │       ├── JobList/              # 任务列表
    │       ├── JobDetails/           # 任务详情
    │       └── BuildDetails/         # 构建详情
    │
    ├── public/                       # 静态资源
    │   └── vite.svg                  # 网站图标
    │
    ├── package.json                  # 前端依赖配置
    ├── tsconfig.json                 # TypeScript 配置
    ├── vite.config.ts                # Vite 配置
    └── index.html                    # HTML 模板
```

## 📝 文件说明

### 后端核心文件

#### `app.ts`
Express 应用主入口文件,配置:
- 中间件
- 路由
- Socket.IO
- 错误处理
- CORS

#### `jenkins.service.ts`
Jenkins API 封装,提供:
- 获取任务列表
- 任务详情查询
- 触发构建
- 停止构建
- 获取构建日志
- 创建/删除任务

#### `auth.service.ts`
认证服务,提供:
- 用户登录
- 用户注册
- Token 生成
- Token 验证
- 密码加密

#### `job.controller.ts`
任务控制器,处理:
- 任务 CRUD 操作
- 构建管理
- 日志查询

#### `auth.controller.ts`
认证控制器,处理:
- 登录请求
- 注册请求
- 用户信息查询
- Token 刷新

### 前端核心文件

#### `main.tsx`
应用入口,配置:
- React Query
- 路由
- Ant Design
- 全局样式

#### `App.tsx`
根组件,定义:
- 路由结构
- 受保护路由
- 布局组件

#### `client.ts`
HTTP 客户端,实现:
- 请求拦截器
- 响应拦截器
- Token 自动附加
- 错误处理

#### `store/index.ts`
状态管理,管理:
- 用户状态
- 认证状态
- WebSocket 连接状态

## 🔧 配置文件

### `package.json`
依赖管理和脚本配置

### `tsconfig.json`
TypeScript 编译选项

### `vite.config.ts`
Vite 构建配置,包含:
- 路径别名
- 代理配置
- 构建优化

### `.env.example`
环境变量模板,包含:
- Jenkins 连接信息
- JWT 配置
- 服务器端口
- CORS 设置

## 📊 代码统计

| 目录 | 文件数 | 代码行数 |
|------|--------|---------|
| backend/src | 15+ | ~2000 |
| frontend/src | 10+ | ~1500 |
| 配置文件 | 8 | ~300 |
| 文档 | 4 | ~1000 |

## 🎯 关键模块说明

### 认证模块
- **位置**: `backend/src/services/auth.service.ts`
- **功能**: JWT 认证、用户管理
- **依赖**: jsonwebtoken, bcryptjs

### Jenkins 集成模块
- **位置**: `backend/src/services/jenkins.service.ts`
- **功能**: Jenkins API 封装
- **依赖**: axios

### 前端路由模块
- **位置**: `frontend/src/App.tsx`
- **功能**: 路由管理、权限控制
- **依赖**: react-router-dom

### 状态管理模块
- **位置**: `frontend/src/store/index.ts`
- **功能**: 全局状态管理
- **依赖**: zustand

## 📦 依赖关系

### 后端主要依赖
```
express → controllers → services → Jenkins API
          ↓
      middleware → auth/error handling
```

### 前端主要依赖
```
React → Router → Pages → Components
                  ↓
              Store (Zustand)
                  ↓
              API Layer → Backend
```

## 🔄 数据流向

```
用户操作 → React 组件
         ↓
    API 调用 (axios)
         ↓
    后端路由 (Express)
         ↓
    控制器层 (Controller)
         ↓
    业务逻辑 (Service)
         ↓
    Jenkins API
         ↓
    响应返回用户
```
